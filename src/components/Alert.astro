---
interface Props {
    heading: string;
}

const { heading } = Astro.props;
---
<div class="w-full max-w-5xl gap-3 px-3 py-6 bg-zinc-900">
    <h2 class="text-2xl leading-6 font-brand text-zinc-400">{heading}</h2>
    <slot/>
</div>